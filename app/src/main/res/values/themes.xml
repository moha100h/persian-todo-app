<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- تم اصلی اپ -->
    <style name="Theme.PersianTodo" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowLayoutInDisplayCutoutMode">shortEdges</item>
    </style>

    <!-- تم SplashScreen — باید parent از Theme.PersianTodo باشه -->
    <style name="Theme.PersianTodo.SplashScreen" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">#6650A4</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_notification</item>
        <item name="postSplashScreenTheme">@style/Theme.PersianTodo</item>
    </style>
</resources>
